name: Deploy Gerenciamento_Contatos (DISABLED - Render handles deployment)

on:
  push:
    branches:
      - disabled-branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
