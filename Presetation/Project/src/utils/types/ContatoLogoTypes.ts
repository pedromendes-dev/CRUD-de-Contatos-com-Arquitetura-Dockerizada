export interface LogoProps {
  size?: number;
  className?: string;
  showText?: boolean;
  variant?: "default" | "white" | "dark" | "badge";
  textSize?: number;
}

export interface LogoInternaProps {
  size?: number;
  showText?: boolean;
  className?: string;
}
